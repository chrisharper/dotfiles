- clean:
    ~/:

- link:
    ~/.gitconfig : git/config
    ~/.config/nvim :
      create: true
      force: true
      path:  nvim/
    ~/.xinitrc : xinitrc
    ~/.bashrc : bashrc
    ~/.tmux.conf : tmux.conf
