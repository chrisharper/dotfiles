- clean:
    ~/:

- link:
    ~/.config/kitty :
      create: true
      force: true
      path: kitty/
    ~/.config/nvim :
      create: true
      force: true
      path:  nvim/
    ~/.gitconfig : git/config
    ~/.tmux.conf : tmux.conf
    ~/.zprofile : zprofile
