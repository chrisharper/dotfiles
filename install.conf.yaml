- clean:
    ~/:

- link:
    ~/.gitconfig : git/config
    ~/.config/nvim :
      create: true
      force: true
      path:  nvim/
    ~/.tmux.conf : tmux.conf
    ~/.config/kitty :
      create: true
      force: true
      path: kitty/
