- clean:
    ~/:

- link:
    ~/.gitconfig : git/config
    ~/.config/nvim :
      create: true
      force: true
      path:  nvim/
    ~/.tmux.conf : tmux.conf
    ~/.xinitrc : xinitrc
    ~/.bashrc : bashrc
